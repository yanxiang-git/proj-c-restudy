/*
不停的输入整数，显示其和和平均数
*/
#include <stdio.h>

int main(void)
{

	int sum = 0,
	int cnt = 0,
	int retry;

	do{
		int t ;

		puts("输入一个整数: ");
		scanf("%d",&t);

		sum = sum + t;
		cnt = cnt + 1;

		printf("是否继续: ");







	} while(retry == 0);







	return 0;
}